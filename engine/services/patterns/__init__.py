"""SMC + VELO pattern engine.

Modules use sibling absolute imports (e.g. `from smc_detector import ...`),
so run tests from this directory:

    cd engine/services/patterns
    python test_integration.py
"""
